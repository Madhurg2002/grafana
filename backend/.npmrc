# Always install devDependencies (typescript, @types/*) even when
# NODE_ENV=production is set in the environment — the build still needs
# them to compile, and they add no runtime surface.
include=dev
